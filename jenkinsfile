pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                echo 'Hello world!' 
                sh "ls -ltr"
                echo "$BUILD_NUMBER"
            }
        }
/*        stage('Building image') {
            steps {
                script {
                    docker.build registry + ":$BUILD_NUMBER"
                }
            }
        }
        stage('Stage 1') {
            steps {
                docker ps | grep "$BUILD_NUMBER" 
            }
        } */
    }
}
